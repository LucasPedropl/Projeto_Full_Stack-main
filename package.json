{
  "name": "gastometer-fullstack",
  "private": true,
  "version": "2.0.0",
  "description": "GastôMeter — Projeto Fullstack ES47B (frontend + backend)",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "build": "npm run build --prefix frontend",
    "db:seed": "npm run db:seed --prefix backend",
    "docker:up": "docker compose up -d --wait",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f db",
    "docker:reset": "docker compose down -v && docker compose up -d --wait",
    "setup": "npm run install:all && npm run docker:up && npm run db:seed"
  }
}
